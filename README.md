# THE-SELF-HEALING-AI-INGESTION-PIPELINE
Self-healing AI ingestion pipeline built with FastAPI. Validates incoming telemetry data, detects anomalies using Z-score, automatically corrects invalid values, and assigns a confidence score to ensure data quality before downstream processing.
